{% extends "@Wordpress/base.html.twig" %}

{% block body %}
    <div class="post">
        <div class="post-title">
            <h1>{{ page.title }}</h1>
        </div>
        <div class="post-excerpt">
            {{ page.excerpt|raw }}
        </div>
    </div>
{% endblock %}
